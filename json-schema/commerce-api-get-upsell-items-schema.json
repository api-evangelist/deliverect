{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/deliverect/refs/heads/main/json-schema/commerce-api-get-upsell-items-schema.json",
  "title": "GetUpsellItems",
  "description": "Deliverect Get Upsell Items",
  "type": "object",
  "properties": {
    "account": {
      "type": "string",
      "example": "string",
      "description": "The unique identifier of a customer account"
    },
    "channelLink": {
      "type": "string",
      "example": "string",
      "description": "The unique identifier of a channel link within a store location"
    },
    "location": {
      "type": "string",
      "example": "string",
      "description": "The unique identifier of a location within a customer account"
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "plu": {
            "type": "string",
            "example": "PIE1",
            "description": "Unique identifier for a product"
          },
          "quantity": {
            "type": "integer",
            "default": 1,
            "example": 1,
            "description": "Amount of items ordered"
          }
        },
        "required": [
          "plu",
          "quantity"
        ]
      },
      "description": "Items in the order which upsell recommendations will be based on"
    }
  },
  "required": [
    "account",
    "channelLink",
    "location",
    "items"
  ]
}