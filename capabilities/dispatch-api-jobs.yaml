naftiko: 1.0.0-alpha2
info:
  label: Deliverect Dispatch API — Jobs
  description: 'Jobs surface of the Deliverect Dispatch API. 4 operations. Lead operation: Validate Delivery Job. Self-contained Naftiko capability covering one Deliverect business surface.'
  tags:
  - Deliverect
  - Dispatch
  - Jobs
  created: '2026-06-02'
  modified: '2026-06-02'
binds:
- namespace: env
  keys:
    DELIVERECT_TOKEN: DELIVERECT_TOKEN
capability:
  consumes:
  - type: http
    namespace: dispatch-api-jobs
    baseUri: https://api.deliverect.com
    description: Deliverect Dispatch API — Jobs business capability. Self-contained, no shared references.
    authentication:
      type: bearer
      token: '{{env.DELIVERECT_TOKEN}}'
    resources:
    - name: validate-job
      path: /validate_job
      operations:
      - name: dispatch_validate_job
        method: POST
        description: Deliverect Validate Delivery Job
        inputParameters: []
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: dispatch-create-job
      path: /dispatch/create_job
      operations:
      - name: dispatch_create_job
        method: POST
        description: Deliverect Create Delivery Job
        inputParameters:
        - name: body
          in: body
          type: object
          required: true
          description: Request payload.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: dispatch-update-job
      path: /dispatch/updateJob
      operations:
      - name: dispatch_updatejob
        method: POST
        description: Deliverect Copy of Cancel Delivery Job
        inputParameters:
        - name: body
          in: body
          type: object
          required: true
          description: Request payload.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: dispatch-cancel-job
      path: /dispatch/cancel_job
      operations:
      - name: dispatch_cancel_job
        method: POST
        description: Deliverect Cancel Delivery Job
        inputParameters:
        - name: body
          in: body
          type: object
          required: true
          description: Request payload.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: dispatch-api-jobs-rest
    port: 8080
    description: REST adapter for Deliverect Dispatch API — Jobs. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/validate-job
      name: validate-job
      description: REST surface for validate-job.
      operations:
      - method: POST
        name: dispatch_validate_job
        description: Deliverect Validate Delivery Job
        call: dispatch-api-jobs.dispatch_validate_job
        with: {}
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/dispatch/create-job
      name: dispatch-create-job
      description: REST surface for dispatch-create-job.
      operations:
      - method: POST
        name: dispatch_create_job
        description: Deliverect Create Delivery Job
        call: dispatch-api-jobs.dispatch_create_job
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/dispatch/update-job
      name: dispatch-update-job
      description: REST surface for dispatch-update-job.
      operations:
      - method: POST
        name: dispatch_updatejob
        description: Deliverect Copy of Cancel Delivery Job
        call: dispatch-api-jobs.dispatch_updatejob
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/dispatch/cancel-job
      name: dispatch-cancel-job
      description: REST surface for dispatch-cancel-job.
      operations:
      - method: POST
        name: dispatch_cancel_job
        description: Deliverect Cancel Delivery Job
        call: dispatch-api-jobs.dispatch_cancel_job
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: dispatch-api-jobs-mcp
    port: 9090
    transport: http
    description: MCP adapter for Deliverect Dispatch API — Jobs. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: validate-delivery-job
      description: Deliverect Validate Delivery Job
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: dispatch-api-jobs.dispatch_validate_job
      with: {}
      outputParameters:
      - type: object
        mapping: $.
    - name: create-delivery-job
      description: Deliverect Create Delivery Job
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: dispatch-api-jobs.dispatch_create_job
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: copy-cancel-delivery-job
      description: Deliverect Copy of Cancel Delivery Job
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: dispatch-api-jobs.dispatch_updatejob
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: cancel-delivery-job
      description: Deliverect Cancel Delivery Job
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: dispatch-api-jobs.dispatch_cancel_job
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
